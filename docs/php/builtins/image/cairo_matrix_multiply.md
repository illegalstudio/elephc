---
title: "cairo_matrix_multiply()"
description: "Implemented by the compiler-injected image prelude."
sidebar:
  order: 416
---

## cairo_matrix_multiply()

```php
function cairo_matrix_multiply(mixed $m1, mixed $m2): mixed
```

Implemented by the compiler-injected image prelude.

**Parameters**:
- `$m1` (`mixed`)
- `$m2` (`mixed`)

**Returns**: `mixed`

## Availability

- **Compiled (AOT)**: supported through the compiler-injected image prelude.
- **`eval()` (magician interpreter)**: not available inside eval'd code.

_No examples yet — check `examples/` and `showcases/` for usage patterns._

## Internals

For how `cairo_matrix_multiply` is implemented in the compiler, see [the internals page](../../../internals/builtins/image/cairo_matrix_multiply.md).
