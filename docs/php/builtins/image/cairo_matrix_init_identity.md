---
title: "cairo_matrix_init_identity()"
description: "Creates an identity transformation matrix."
sidebar:
  order: 417
---

## cairo_matrix_init_identity()

```php
function cairo_matrix_init_identity(): mixed
```

Creates an identity transformation matrix.

**Parameters**: none.

**Returns**: `mixed`

## Availability

- **Compiled (AOT)**: supported through the compiler-injected image prelude.
- **`eval()` (magician interpreter)**: not available inside eval'd code.

_No examples yet. Check `examples/` and `showcases/` for usage patterns._

## Internals

For how `cairo_matrix_init_identity` is implemented in the compiler, see [the internals page](../../../internals/builtins/image/cairo_matrix_init_identity.md).
