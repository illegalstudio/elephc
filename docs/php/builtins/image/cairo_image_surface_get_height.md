---
title: "cairo_image_surface_get_height()"
description: "Returns an image surface's height in pixels."
sidebar:
  order: 414
---

## cairo_image_surface_get_height()

```php
function cairo_image_surface_get_height(mixed $surface): int
```

Returns an image surface's height in pixels.

**Parameters**:
- `$surface` (`mixed`)

**Returns**: `int`

## Availability

- **Compiled (AOT)**: supported through the compiler-injected image prelude.
- **`eval()` (magician interpreter)**: not available inside eval'd code.

_No examples yet. Check `examples/` and `showcases/` for usage patterns._

## Internals

For how `cairo_image_surface_get_height` is implemented in the compiler, see [the internals page](../../../internals/builtins/image/cairo_image_surface_get_height.md).
