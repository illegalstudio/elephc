---
title: "mysqli_insert_id()"
description: "Returns the id generated by the last INSERT on an AUTO_INCREMENT column."
sidebar:
  order: 140
---

## mysqli_insert_id()

```php
function mysqli_insert_id(mixed $mysql): int
```

Returns the id generated by the last INSERT on an AUTO_INCREMENT column.

**Parameters**:
- `$mysql` (`mixed`)

**Returns**: `int`

## Availability

- **Compiled (AOT)**: supported through an injected elephc-PHP prelude.
- **`eval()` (magician interpreter)**: not available inside eval'd code.

_No examples yet. Check `examples/` and `showcases/` for usage patterns._

## Internals

For how `mysqli_insert_id` is implemented in the compiler, see [the internals page](../../../internals/builtins/database/mysqli_insert_id.md).
