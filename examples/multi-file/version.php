<?php
// A small library file: defines a helper. It has no top-level `return`, so a
// `require` of it yields 1 (PHP's default for a successful include).
function library_version(): string {
    return "1.0.0";
}
