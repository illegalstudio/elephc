# php-src timelib provenance

These files are copied verbatim from `ext/date/lib` at php-src commit
`47b563cbb856ec19155aacc3246931dfacbebd21` on the `PHP-8.5` branch.

The generated timezone database identifies itself as `2026.3`. Regenerate this
directory only from a new explicitly audited php-src commit, and update this
record and the datetime compliance specification together.
