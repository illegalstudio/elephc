# Third-party data

The Rust implementation is part of Elephc and uses the repository's MIT license.

The generated Unicode Character Database tables under `src/unicode/data` use
Unicode License V3. Retain the accompanying `LICENSE-UNICODE` when distributing
those tables or binaries containing them.

The detection bitmap under `src/detect/data` derives from PHP 8.5.10's
`ext/mbstring/common_codepoints.txt`. Its source and output hashes are recorded in
`manifest.json`. This data uses the PHP License, version 3.01; retain the accompanying
`LICENSE-PHP` in source and binary distributions.

This product includes PHP software, freely available from
<http://www.php.net/software/>.
